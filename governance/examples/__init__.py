"""Worked domains that use the governance core.

`refunds` is deliberately nothing like a container terminal: a payments
dispute agent, with money instead of minutes, customers instead of vessels,
and its own four-row policy table. It exists to show that the governed-edit
pattern is a pattern and not a description of one project.
"""
