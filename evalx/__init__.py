"""evalx: the chaos and evaluation suite for RELAY.

Modules: harness (tau2-style case runner + oracle gate), scorecard (the MGF
scorecard renderer), sweep_local (the overnight Mac sweep runner with
checkpoint/resume). SOP: policy.md; cases: tasks.json; hand-computed expected
numbers: oracle_pack.{md,json}. All data SYNTHETIC.
"""
